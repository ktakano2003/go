name: Go Test

on:
    push:

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/chackout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    - name: Build
      run: go build -v .
